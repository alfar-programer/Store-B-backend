# Environment Configuration

# Database Configuration
DB_HOST=mysql-73b2b04-mazenalfar01.h.aivencloud.com
DB_PORT=23199
DB_NAME=defaultdb
DB_USER=avnadmin
DB_PASSWORD=your_database_password_here

# JWT Secret (IMPORTANT: Use a strong, random secret in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here_minimum_32_characters_long

# Admin Credentials (for initial admin user creation)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=YourSecurePassword123!

# Environment
NODE_ENV=development

# Frontend URLs (for CORS configuration)
FRONTEND_URL=https://your-frontend-domain.com
ADMIN_URL=https://your-admin-dashboard-domain.com

# Server Port
PORT=5000

# Cloudinary Configuration (for image uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name_here
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here

# ===========================
# RESEND EMAIL CONFIGURATION
# ===========================
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here

# Email sender address (must be verified in Resend)
# For development, you can use: onboarding@resend.dev
# For production, use your verified domain: noreply@yourdomain.com
EMAIL_FROM=onboarding@resend.dev

# Optional: Disable email verification for development (true/false)
# When set to false, users will be auto-verified without needing email confirmation
EMAIL_VERIFICATION_ENABLED=true

# ===========================
# GOOGLE OAUTH CONFIGURATION
# ===========================
# Get your Client ID from Google Cloud Console → Credentials
# See GOOGLE_OAUTH_SETUP.md for detailed setup instructions
GOOGLE_CLIENT_ID=123456789-abcdefghijklmnop.apps.googleusercontent.com

# Security Notes:
# 1. Never commit the actual .env file to version control
# 2. Use strong, unique passwords for production
# 3. Generate a cryptographically secure JWT_SECRET
# 4. Keep database credentials secure
# 5. Update FRONTEND_URL and ADMIN_URL with your actual domains
# 6. Get Resend API key from https://resend.com and verify your domain